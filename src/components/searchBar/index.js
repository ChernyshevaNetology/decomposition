import React from "react";

/**
 * Компонент ввода поиска
 */

const SearchBar = () => {
  return (
    <div>
      <input type="search" label="yandex найдется все" />
    </div>
  );
};

export default SearchBar;
